import { AlertComponentDirective } from './alert-component.directive';

describe('AlertComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertComponentDirective();
    expect(directive).toBeTruthy();
  });
});
